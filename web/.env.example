# Local dev: http://localhost:4000/api — Vercel/production: /api (never localhost or the live site will call your machine)
VITE_API_URL=/api
VITE_SOLANA_RPC=https://api.devnet.solana.com
VITE_SOLANA_NETWORK=devnet
VITE_USDC_MINT_ADDRESS=4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU
VITE_ANALYTICS_DASHBOARD_URL=

# Pinata JWT (scoped upload key). Same value as backend PINATA_JWT — required on Vercel for product uploads.
# Local: enables browser→Pinata so large files skip localhost:4000. Production: set in Vercel env + redeploy web build.
VITE_PINATA_JWT=
